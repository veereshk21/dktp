/**
 * Created by hmahad on 5/16/2017.
 */
/**
 * Main reducer which sets the initial data for pastdue page.
 * @param state
 * @param action
 * @returns {{}}
 */

export const pastDueData = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
